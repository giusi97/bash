# filtra_g.sh

Script Bash di esempio che legge un file di testo contenente nomi (uno per riga)
e stampa solo quelli che iniziano con la lettera **g** (minuscola).

## 📌 Scopo

Questo script è pensato come esercizio introduttivo allo scripting Bash
e dimostra:

- gestione degli argomenti da riga di comando
- controlli sugli input
- lettura di file riga per riga
- filtraggio dei dati
- uso corretto di stdout / stderr
- modalità sicura (`set -euo pipefail`)

## ▶️ Utilizzo

Rendere lo script eseguibile:

```bash
chmod +x filtra_g.sh
